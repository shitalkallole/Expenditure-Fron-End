import { BetweenDatesRequestForLendBorrow } from './between-dates-request-for-lend-borrow';

describe('BetweenDatesRequestForLendBorrow', () => {
  it('should create an instance', () => {
    expect(new BetweenDatesRequestForLendBorrow()).toBeTruthy();
  });
});
