import { LendBorrowExpenseRequest } from './lend-borrow-expense-request';

describe('LendBorrowExpenseRequest', () => {
  it('should create an instance', () => {
    expect(new LendBorrowExpenseRequest()).toBeTruthy();
  });
});
