import { BetweenDatesRequestForCategory } from './between-dates-request-for-category';

describe('BetweenDatesRequestForCategory', () => {
  it('should create an instance', () => {
    expect(new BetweenDatesRequestForCategory()).toBeTruthy();
  });
});
