import { UserPasswordUpdate } from './user-password-update';

describe('UserPasswordUpdate', () => {
  it('should create an instance', () => {
    expect(new UserPasswordUpdate()).toBeTruthy();
  });
});
