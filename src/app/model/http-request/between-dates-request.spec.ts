import { BetweenDatesRequest } from './between-dates-request';

describe('BetweenDatesRequest', () => {
  it('should create an instance', () => {
    expect(new BetweenDatesRequest()).toBeTruthy();
  });
});
