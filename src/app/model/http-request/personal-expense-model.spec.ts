import { PersonalExpenseModel } from './personal-expense-model';

describe('PersonalExpenseModel', () => {
  it('should create an instance', () => {
    expect(new PersonalExpenseModel()).toBeTruthy();
  });
});
