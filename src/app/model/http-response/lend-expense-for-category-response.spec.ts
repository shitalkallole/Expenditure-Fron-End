import { LendExpenseForCategoryResponse } from './lend-expense-for-category-response';

describe('LendExpenseForCategoryResponse', () => {
  it('should create an instance', () => {
    expect(new LendExpenseForCategoryResponse()).toBeTruthy();
  });
});
